{
  "admin_email": "${ADMIN_EMAIL}",
  "_comment_smtp": "Configurazione per il server SMTP per l'invio di notifiche email.",
  "smtp": {
    "host": "${SMTP_HOST}",
    "port": "${SMTP_PORT}",
    "username": "${SMTP_USERNAME}",
    "password": "${SMTP_PASSWORD}"
  },
  "workflows": [
    {
      "id": "example-zap",
      "_comment_trigger": "Questo trigger controlla la casella Gmail a intervalli regolari.",
      "trigger": {
        "type": "gmail_poll",
        "_comment_query0": "La query utilizza gli operatori di ricerca standard di Gmail. Questa cerca email non lette, non speciali e con allegato da un mittente specifico.",
        "_comment_query1": "opzioni: label:inbox is:unread is:starred has:attachment (-is:starred OR -has:attachment) {-is:starred -has:attachment}",
        "query": "from:pierino.centrella@gmail.com label:inbox is:unread",
        "max_results": 10,
        "interval": 60
      },
      "actions": [
        {
          "type": "g_drive_upload",
          "params": {"folder_id": "${GDRIVE_FOLDER_ID}"}
        },
        {"type": "sheets_append", "params": {"sheet_id": "${GSHEET_ID}", "range": "Foglio1!A:B"}},
        {"type": "slack_notify", "params": {"webhook_url": "${SLACK_WEBHOOK_URL}"}}
      ]
    }
  ]
}
